---
# This is an example playbook to execute Ansible tests.

- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - name: Example assertion
      ansible.builtin.assert:
        that: true

    - name: Zabbix agent has started
      ansible.builtin.service:
        service: zabbix-agent
        state: started
